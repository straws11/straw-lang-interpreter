struct Point {
	int x,
	int y
}

func move_point(Point p, int dx, int dy) -> Point {
	return Point {
		x = p.x + dx,
		y = p.y + dy
	}
}

let p = Point { x = 1, y = 2 }
let p2 = move_point(p, 2, 3)

print(int_to_str(p2.x))
print(int_to_str(p2.y))
